;; extends

; Add custom group to emphasis markers so that we can target them for
; highlighting
(emphasis (emphasis_begin) @punctuation.delimiter.italic)
(emphasis (emphasis_end) @punctuation.delimiter.italic)
